{
  "devDependencies": {
    "babel-plugin-external-helpers": "^6.22.0",
    "babel-preset-es2015-rollup": "^3.0.0",
    "eslint": "^4.12.0",
    "jest": "^21.2.1",
    "rollup-plugin-babel": "^3.0.2",
    "rollup-plugin-uglify": "^2.0.1"
  },
  "scripts": {
    "test": "jest",
    "watch": "rollup -c -w",
    "dev": "rollup -c --environment entry:",
    "prod": "rollup -c --environment entry:dev"
  },
  "jest": {
    "transform": {}
  },
  "dependencies": {
    "axios": "^0.17.1"
  }
}
